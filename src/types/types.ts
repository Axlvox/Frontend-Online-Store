export type ProductType = {
  id: string;
  title: string;
  thumbnail: string;
  price: number;
};

export type ProductDetailProps = {
  title: string;
  image: string;
  price: string;
  detailLink: string;
};
